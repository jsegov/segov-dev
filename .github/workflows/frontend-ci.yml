name: Frontend CI

on:
  push:
    branches: [main, master]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'turbo.json'
  pull_request:
    branches: [main, master]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'turbo.json'

concurrency:
  group: frontend-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    environment: Github Actions
    timeout-minutes: 60
    permissions:
      contents: read
    env:
      AI_GATEWAY_API_KEY: ${{ secrets.AI_GATEWAY_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm --filter frontend format:check

      - name: Lint
        run: pnpm --filter frontend lint

      - name: Typecheck
        run: pnpm --filter frontend typecheck

      - name: Unit tests
        run: pnpm --filter frontend test

      - name: Build
        run: pnpm --filter frontend build

      - name: Install browser
        run: pnpm --filter frontend exec playwright install --with-deps chromium

      - name: Browser smoke tests (synthetic network fixtures)
        id: browser-smoke
        env:
          PLAYWRIGHT_OUTPUT_DIR: ${{ runner.temp }}/segov-dev-playwright
        run: pnpm --filter frontend test:browser

      - name: Upload browser failure traces
        if: ${{ always() && steps.browser-smoke.outcome == 'failure' }}
        uses: actions/upload-artifact@v4
        with:
          name: browser-smoke-results
          path: ${{ runner.temp }}/segov-dev-playwright
          if-no-files-found: ignore

      - name: AMA live evals
        id: ama-evals
        if: ${{ env.AI_GATEWAY_API_KEY != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
        env:
          AMA_EVAL_PROFILE: production
          AMA_CHAT_MODEL: ${{ vars.AMA_CHAT_MODEL }}
          AMA_CHAT_PROVIDERS: ${{ vars.AMA_CHAT_PROVIDERS }}
          AMA_MAX_OUTPUT_TOKENS: ${{ vars.AMA_MAX_OUTPUT_TOKENS }}
          AMA_INFERENCE_BASE_URL: ${{ secrets.AMA_INFERENCE_BASE_URL || vars.AMA_INFERENCE_BASE_URL }}
          AMA_DEPLOYMENT_MODEL: ${{ vars.AMA_DEPLOYMENT_MODEL }}
          AMA_INFERENCE_REASONING_EFFORT: ${{ vars.AMA_INFERENCE_REASONING_EFFORT }}
          AMA_INFERENCE_API_KEY: ${{ secrets.AMA_INFERENCE_API_KEY }}
          AMA_INFERENCE_HEADERS: ${{ secrets.AMA_INFERENCE_HEADERS }}
        run: pnpm --filter frontend eval:ama:ci

      - name: Skip AMA live evals
        if: ${{ env.AI_GATEWAY_API_KEY == '' || (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) }}
        run: echo "Skipping AMA live evals because AI Gateway credentials are unavailable for this run."

      - name: Upload AMA eval results
        if: ${{ always() && steps.ama-evals.outcome != 'skipped' }}
        uses: actions/upload-artifact@v4
        with:
          name: ama-eval-results
          path: frontend/evals/ama/results/latest.json
          if-no-files-found: ignore
