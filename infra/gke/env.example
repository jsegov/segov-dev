# GKE deployment parameters for CI/CD
GCP_PROJECT=segov-dev-model
GKE_CLUSTER_NAME=segov-dev-model-cluster
GKE_CLUSTER_LOCATION=us-central1
GKE_STATIC_IP_NAME=segov-dev-static-ip
GKE_DOMAIN=model.segov.dev

# Required for Workload Identity binding (KSA -> GCP SA)
VLLM_GCP_SERVICE_ACCOUNT=<your-sa>@<project>.iam.gserviceaccount.com

# Optional: pick model (default is Qwen/Qwen3-8B-FP8)
# Uncomment to use Qwen3-14B as specified in docs/gke_openai_migration.md
# VLLM_MODEL_ID=Qwen/Qwen3-14B

# Optional: leave empty to skip Cloud Armor BackendConfig
GKE_ARMOR_POLICY=

# Note: ARTIFACT_REGISTRY_HOST/REPOSITORY are used by build workflows, not infra deploy script
ARTIFACT_REGISTRY_HOST=us-central1-docker.pkg.dev
ARTIFACT_REGISTRY_REPOSITORY=segov-dev-gke-images

# Legacy/unused variables
# GCS_BUCKET_NAME - Not currently used by deploy_gke.sh (uses PVC instead)
GCS_BUCKET_NAME=segov-dev-model-bucket
