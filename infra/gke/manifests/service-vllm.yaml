apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: inference
  annotations:
    cloud.google.com/backend-config: '${BACKEND_CONFIG_ANNOTATION}'
spec:
  selector:
    app: vllm
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
