#!/usr/bin/env bash
set -euo pipefail

# Placeholder script to orchestrate helm/kubectl (filled in later tasks)
echo "Deploying infra (stub)"
# kubectl apply -f infra/manifests/huggingface-model-cache-pvc.yaml
# helm upgrade --install basic-inference coreweave/vllm-inference \
#   --namespace inference --create-namespace \
#   -f infra/helm-values/vllm-qwen-values.yaml

